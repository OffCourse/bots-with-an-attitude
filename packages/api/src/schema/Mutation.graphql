enum APIEventType {
  INITIALIZED
  RESET
}

input Cassette {
  name: String
}

input BWAConfig {
  cassettes: [Cassette]!
  botName: String!
}

input BWAEvent {
  eventType: APIEventType!
  payload: BWAConfig
}


type Mutation {
  sendEvent(event: BWAEvent!): Status
}
